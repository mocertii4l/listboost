# Screenshot Tour

Preview screenshots should be captured from the Railway preview environment before merging.

Required pages:

- Landing `/` light, dark, mobile
- Pricing `/pricing` light, dark, mobile
- Example `/example` light, dark, mobile
- Login `/login` light, dark, mobile
- Signup `/signup` light, dark, mobile
- App `/app` light, dark, mobile
- Notes `/app/notes` light, dark, mobile
- Checkout success `/checkout/success` light, dark, mobile

Store PNG files in this directory using:

```text
page-theme-viewport.png
```
